# XVPN Panel v1.2.0

正式发布版。

本版本在已完成的测试线基础上收口，重点完善管理员体系、Android 流量上报、首页流量概览、备份安全、列表分页与后台交互体验。节点测速仍由客户端负责，Panel 不执行节点延迟探测。

## 主要更新

### 管理员与登录体系

- 后台登录页管理员用户名默认留空，不再固定显示 `admin`。
- 系统设置支持修改管理员用户名，修改时需要当前密码验证。
- 管理员用户名持久化，Panel 重启、服务器重启和升级后不会恢复为 `admin`。
- `xvpn password` 只重置管理员密码，不修改管理员用户名。
- Android `/api/v1/login` 支持管理员账户登录，管理员 App Token 与普通用户 Token 独立存储。
- 普通用户注册会避开当前管理员用户名。
- 后台网页登录增加失败次数限制：同一来源连续失败达到阈值后临时锁定。

### Android 流量上报与统计

- 新增 Android 用户流量上报接口与汇总接口。
- Android 使用 `device_id + session_id + node_id + 累计上传/下载计数器` 上报。
- Panel 只计算单调累计计数器的增量，重复请求不会重复累计。
- 节点切换、新会话或计数器回退时重新建立 baseline，避免流量归属错误。
- 支持今日、本月、累计用户流量，以及按用户/节点/日期聚合的节点流量。
- 首页新增“今日流量概览”，支持用户排行 / 节点排行 TOP 5 无刷新切换。
- 用户管理显示今日、本月、累计和最后上报时间，并提供独立流量详情。
- 用户管理新增“退出所有设备”和“重置流量”。重置会同步清除节点聚合与设备 baseline。
- Panel 时区统一控制“今日 / 本月”边界、后台时间与自动备份时间。

### 备份与运行记录

- 主按钮统一为“手动备份一次”，生成后在备份记录中下载 / 恢复 / 删除。
- 自动备份继续按保留数量轮换；手动备份不会自动删除。
- 备份记录分页显示，避免长期使用后页面无限拉长。
- 备份文件不保存普通用户 App Token、管理员 App Token 和登录限速状态；恢复旧兼容备份时也会再次清理这些状态。
- 运行状态新增流量上报状态和最近流量上报时间。
- 运行记录采用折叠展示并设置保留上限；“清除记录”入口直接可见，清理日志不会删除备份文件或流量汇总。

### 用户、邀请码与后台交互

- 用户管理支持用户名搜索、状态筛选与分页。
- 邀请码支持搜索、状态筛选与分页。
- 修复邀请码注册开关保存后自动恢复的问题。
- Telegram 设置操作重新归组，清除配置不再单独占用大块布局。
- 保存、备份、恢复、删除、Telegram 测试、创建邀请码等操作增加处理中状态并防止重复提交。
- 双栏 / Grid 卡片统一独立高度；展开、错误提示或内容增长不会拉伸旁边卡片。
- 折叠、分页、空状态、危险操作确认、Hover / Active 反馈与移动端布局统一优化。
- 品牌标识由 `V` 统一调整为 `X`。

### 产品与命令统一

- 产品名称统一为 `XVPN Panel`。
- 管理命令统一为：`xvpn`、`xvpn check`、`xvpn update`、`xvpn domain`、`xvpn status`、`xvpn restart`、`xvpn logs`、`xvpn password`、`xvpn version`。
- 运行路径、数据目录、systemd 服务、备份 timer 与 CLI 统一使用 `xvpn-panel` / `xvpn` 命名。
- 旧 `vpn-panel` 安装升级时继续保留节点、用户、邀请码、管理员账号、数据库、域名、HTTPS、Telegram、备份配置和加密密钥，并迁移到 XVPN 路径。
- CLI 顶部与更新菜单不再展示仓库名称，只显示产品状态和“检查更新”。

## Android 对接要求

- VPN 连接成功后立即上报一次 baseline。
- 连接期间建议约每 5 分钟上报一次，并在断开连接前补报一次。
- 每次新的 VPN/Core 连接生成新的 `session_id`；切换节点后使用新的 `session_id + node_id` 建立新 baseline。
- 同一设备的流量上报建议串行发送。
- `node_id` 必须使用 Panel `/nodes` 或 `/app/bootstrap` 返回的节点整数 ID。
- 节点测速继续由 Android 客户端完成，Panel 不承担节点测速任务。

## 升级说明

- 支持现有 XVPN Panel 原地升级，安装器会保留数据库、管理员账号、加密密钥、域名 / HTTPS、Telegram 与备份配置。
- 安装器在覆盖应用代码前会先备份现有安全配置与数据库。
- 正式版本号、ZIP 文件名、ZIP 根目录、后台显示、API 返回与安装器输出统一为 `1.2.0`。
